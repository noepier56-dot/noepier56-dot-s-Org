// Servicio de IA eliminado temporalmente.
export const generateSmartCopy = async (topic: string, colors: { primary: string; secondary: string }) => {
    return "Funcionalidad de IA desactivada.";
};